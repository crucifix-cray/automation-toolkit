# Mega Fix — 2026-09-03

## Issue
`rclone` `mega:` `lsd`/`ls` hung at `Using previously stored session ID` no reply, `g.api.mega.co.nz` `IPv6` `Network is unreachable` for `2a0b:e46:1:100::11-16`, `g` `41.142.27.203:443` `Trying...` timeout. `Mega` appeared fucked, but `eu.api.mega.co.nz` `sid=YHpE...` `a:"f",c:1` `curl -4` `eu` `200` `f` `22877` nodes proves `Mega` not fucked, just `g` `IPv6` dead.

## Fix
`rclone.conf` `endpoint = https://eu.api.mega.co.nz/` + `LD_PRELOAD='' https_proxy=socks5://127.0.0.1:40000` `rclone` (WARP `127.0.0.1:40000` only for `rclone` `eu` `a:"f"` `200`, not whole computer).

```
[mega]
type = mega
user = emilypeterson30@mail.findmeghana.org
pass = AIjpeMEdPQWNTQHR6YYDYjcEoGFSOGHASO5DjwkHcXUW7iDLFg
session_id = YHpE8zZFzThFIYjGGm44xFcyUGl1YWtCWlE4_HnRwxFodO1IlI4aFoyFUg
master_key = s6SFGB0f4UZk7VYPwK/k3A==
endpoint = https://eu.api.mega.co.nz/
```

`LD_PRELOAD='' https_proxy=socks5://127.0.0.1:40000 rclone lsd mega:railway_sessions` → `114` dirs `session-100` `railway_cli_sessions/` `200`, `rclone lsd mega:` `15` top dirs, `rclone ls mega:brightdata` `15` files, `rclone ls mega:chimera/zenrows` `4` files now `exit 0` via `eu` `WARP` `socks5` only for `rclone` endpoint, not `g` `raw` `banned`.

`rclone cat`/`copy` for `w.api.mega.co.nz` `gfs` still `EOF`/`403` on `http` `gfs206n143` `94.24.37.53` even via `WARP`, but `ls`/`lsd` now works for all.

## Current
- `railway_sessions` `114/500` (`holy-farm-v4` checkpoint)
- `lovable_sessions` `36`, `chimera` `2` + `zenrows` `4` keys (`3a6a9ee...`/`b71908...`/`a71406...`)
- `BD` `12` `0 healthy` (`10 susp` `2 timeout` as of `2026-08-31`), `ZenRows` `2` keys `402` `AUTH004` on `3a6a9ee` (usage exceeded) but `b71908` `200` `109.222.170.164` `FR` `Orange` works
- `VPN` on `192.168.100.39` `WARP` `40000` fixes `rclone` `eu` `f`, no need for whole `computer` `ALL_PROXY`

## Next
Use `rclone --config /tmp/rclone_eu.conf` with `https_proxy=socks5://127.0.0.1:40000` for `mega:` only, keep `LD_PRELOAD=''` raw for `lovable`/`railway` browser, `GODEBUG=netdns=go` not needed with `eu` `WARP`.
